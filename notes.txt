Mounting - first time component is rendered to DOM
Unmounting - when removed

Data (props) should only ever flow down the component tree.
generally you want to use props instead of state - use state for isolated use within a component
